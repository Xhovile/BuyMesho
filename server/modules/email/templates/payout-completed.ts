function escapeHtml(value:string){return value.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#39;");}
export function renderPayoutCompletedEmail(data:{sellerName:string;amount:number;currency:string;payoutId:string;orderReference?:string|null;completedAt:string;dashboardUrl:string;}){const amount=`${data.amount.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})} ${data.currency}`;const text=[`Hello ${data.sellerName},`,`Your payout of ${amount} has been completed.`,`Payout reference: ${data.payoutId}`,...(data.orderReference?[`Order reference: ${data.orderReference}`]:[]),`Completed: ${data.completedAt}`,`View payouts: ${data.dashboardUrl}`,"","BuyMesho"].join("\n");const html=`<div style="font-family:Arial,Helvetica,sans-serif;line-height:1.6;color:#111827"><h2>Payout completed</h2><p>Hello ${escapeHtml(data.sellerName)},</p><p>Your payout of <strong>${escapeHtml(amount)}</strong> has been completed.</p><p><strong>Payout reference:</strong> ${escapeHtml(data.payoutId)}<br/>${data.orderReference?`<strong>Order reference:</strong> ${escapeHtml(data.orderReference)}<br/>`:""}<strong>Completed:</strong> ${escapeHtml(data.completedAt)}</p><p><a href="${escapeHtml(data.dashboardUrl)}" style="display:inline-block;background:#111827;color:#fff;text-decoration:none;padding:12px 18px;border-radius:10px;font-weight:700">View payouts</a></p></div>`;return{text,html};}
